package kz.package_tracking.model;

/**
 * Enumeration of package types available in the system.
 * Each type has different pricing and delivery characteristics.
 */
public enum PackageType {
    STANDARD,
    EXPRESS
}
